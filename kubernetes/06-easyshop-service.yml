apiVersion: v1
kind: Service
metadata:
  name: easyshop-svc
  namespace: easyshop
  labels:
    app: easyshop
spec:
  type: NodePort
  selector:
    app: easyshop
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
